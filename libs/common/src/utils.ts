export const countNumberOfInstanceBaseOnView = (numberOfView: number): number => {
  // some how calculate the number of instance base on the view and the available of accounts with status free
  return 3
}
